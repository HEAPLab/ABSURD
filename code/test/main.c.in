#include <stdio.h>
#include <time.h>

#include "user.h"
#include "simple_random.h"


#define BENCH_NAME ${BENCH_NAME}
#define BENCH_NAME_STR "${BENCH_NAME}"

#define SEED 123U

extern void BENCH_NAME(UINT32_T seed);

extern double result;

int main() {

    printf("***************** HEAP WCET Benchmarks *****************\n");
    printf("Running with:\nITERATIONS:\t%d\nARRAY_LENGTH:\t%d\nSEED:\t\t%d\n",ITERATIONS,ARRAY_LENGTH,SEED);
    printf("********************************************************\n");

    BENCH_NAME(SEED);

    printf("[" BENCH_NAME_STR "]\tExecution time:\t%.0f ns\n", result);

    return 0;    
}
